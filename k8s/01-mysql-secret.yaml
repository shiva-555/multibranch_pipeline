# Shared DB credentials used by BOTH student-service and teacher-service.
# Replace with a real secret (or better: use AWS Secrets Manager / External Secrets)
# before deploying to a real environment. Do not commit real secrets to git.
apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  namespace: student-teacher-portal
type: Opaque
stringData:
  host: mysql-service
  user: root
  password: mysql123
  database: school
