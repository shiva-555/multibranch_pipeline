apiVersion: apps/v1
kind: Deployment
metadata:
  name: student-service
  namespace: student-teacher-portal
  labels:
    app: student-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: student-service
  template:
    metadata:
      labels:
        app: student-service
    spec:
      containers:
        - name: student-service
          image: <ECR_REPO_URI>/student-service:<IMAGE_TAG>   # replaced by CI/CD (Jenkins)
          ports:
            - containerPort: 3501
          env:
            - name: host
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: host
            - name: user
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: user
            - name: password
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: password
            - name: database
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: database
            - name: PORT
              value: "3501"
          readinessProbe:
            httpGet:
              path: /health
              port: 3501
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3501
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: student-service
  namespace: student-teacher-portal
spec:
  selector:
    app: student-service
  ports:
    - port: 3501
      targetPort: 3501
  type: ClusterIP
