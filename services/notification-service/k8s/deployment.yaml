apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: student-teacher-portal
  labels:
    app: notification-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
        - name: notification-service
          image: <ECR_REPO_URI>/notification-service:<IMAGE_TAG>   # replaced by CI/CD (Jenkins)
          ports:
            - containerPort: 3504
          env:
            - name: host
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: host
            - name: user
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: user
            - name: password
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: password
            - name: database
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: database
            - name: PORT
              value: "3504"
          readinessProbe:
            httpGet:
              path: /health
              port: 3504
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3504
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: student-teacher-portal
spec:
  selector:
    app: notification-service
  ports:
    - port: 3504
      targetPort: 3504
  type: ClusterIP
